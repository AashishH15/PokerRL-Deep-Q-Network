# Contributing

1. Open an issue for bugs or feature ideas before large changes.
2. Use a virtual environment and `pip install -r requirements.txt`.
3. Run tests: `python -m unittest discover -s tests -v`
4. Do not commit model checkpoints, logs, or `.npz` training data.

Pull requests welcome for docs, tests, and training improvements.
